package com.amalhanaja.sani.data

/**
 * Created by doy on 11/5/17.
 * Email : amalhanaja@gmail.com
 * Created On : 11/5/17.
 */

//fun DriverEntity.mapFromEntity(): User =
//        User(uuid, username, firstName, lastName, phoneNumber?.toLong(), deviceId, vehicleId,
//                ISODateTimeFormat.dateTimeParser().parseDateTime(createdAt).toDate(),
//                ISODateTimeFormat.dateTimeParser().parseDateTime(lastModified).toDate(),
//                status?.let { UserStatus.valueOf(it) })
//
//fun User.mapToEntity(): DriverEntity =
//        DriverEntity(uuid, username, firstName, lastName, phoneNumber.toString(), deviceId, vehicleId,
//                createdAt.toString(), lastModified.toString(), status.toString())